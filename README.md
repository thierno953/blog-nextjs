```bash
npx create-next-app@latest --typescript

npm run dev

npm install next-themes phosphor-react

npm install --save-dev autoprefixer @svgr/webpack

npm install @next/mdx @mdx-js/loader @mdx-js/react

npm install rehype-highlight rehype-pretty-code remark-frontmatter shiki
```
